# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
coverage.xml
benchmark/checkouts/

# uv — this project's own lock is not checked in, but test fixtures that a
# collector must parse are source, not build output.
uv.lock
!tests/fixtures/**/uv.lock

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Scan outputs
*.aibom.json
inventory.json
# Artifact drop created by the ./aibom launcher
aibom-out/
